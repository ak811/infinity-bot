# cog/spin_wheel/__init__.py
"""
Spin Wheel Cog package.

Contains:
- config_spin.py   (settings, prizes, EV helpers)
- wheel_engine.py  (pure logic for spinning and payouts)
- view_spin.py     (Discord UI View with animation)
- cog_spin.py      (Cog command + cooldown + balance checks)
"""

# Nothing to import here; the cog is loaded via cog_spin.setup()
